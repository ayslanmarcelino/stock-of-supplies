<%= link_to(dashboard_index_path, class: "#{'active-menu' if request.path.include?(dashboard_index_path) || request.path == root_path} list-item-menu list-group-item-action",
                                  id: 'dashboard') do %>
  <div class="menu">
    <div class="icon">
      <i class="bi bi-clipboard-data"></i>
    </div>
    <span><%= t('layouts.application.dashboard') %></span>
  </div>
<% end %>

<% if can?(:read, User) %>
  <%= link_to(admin_users_path, class: "#{'active-menu' if request.path.include?(admin_users_path) || request.path.include?(user_roles_path)} list-item-menu list-group-item-action",
                                id: 'user') do %>
    <div class="menu">
      <div class="icon">
        <i class="bi bi-people"></i>
      </div>
      <span><%= t('admin.users.components.layout.resource') %></span>
    </div>
  <% end %>
<% end %>

<% if can?(:read, Supply) %>
  <%= link_to(supplies_path, class: "#{'active-menu' if request.path.include?(supplies_path)} list-item-menu list-group-item-action",
                             id: 'user') do %>
    <div class="menu">
      <div class="icon">
        <i class="bi bi-capsule"></i>
      </div>
      <span><%= t('supplies.components.layout.resource') %></span>
    </div>
  <% end %>
<% end %>

<% if can?(:read, Stock) %>
  <%= link_to(stocks_path, class: "#{'active-menu' if request.path.include?(stocks_path)} list-item-menu list-group-item-action",
                            id: 'user') do %>
    <div class="menu">
      <div class="icon">
        <i class="bi bi-boxes"></i>
      </div>
      <span><%= t('stocks.components.layout.resource') %></span>
    </div>
  <% end %>
<% end %>

<% if can?(:read, Movement) %>
  <%= link_to(movements_path, class: "#{'active-menu' if request.path.include?(movements_path)} list-item-menu list-group-item-action",
                            id: 'user') do %>
    <div class="menu">
      <div class="icon">
        <i class="bi bi-arrow-left-right"></i>
      </div>
      <span><%= t('movements.components.layout.resource') %></span>
    </div>
  <% end %>
<% end %>
