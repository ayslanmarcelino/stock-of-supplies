<div>
  <h4>
    <%= t("application.#{action_header}") %>
  </h4>
</div>
<div>
  <%= render partial: 'layouts/components/errors',
             locals: { resource: @supply } %>
  <%= form_with(model: @supply) do |form| %>
    <%= render partial: 'supplies/components/form',
               locals: { form: form } %>
    <div class="row mt-3">
      <div class="col-lg-6">
        <%= link_to(t('application.cancel'), supplies_path, class: 'btn -transparent-standard') %>
      </div>
      <div class="col-lg-6">
        <button class="btn btn-secondary">
          <%= t("application.#{action}") %>
        </button>
      </div>
    </div>
  <% end %>
</div>
